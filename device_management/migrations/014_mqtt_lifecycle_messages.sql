-- MQTT lifecycle messages (birth / close / will) — Node-RED style

ALTER TABLE loadcell.mqtt_connections
    ADD COLUMN IF NOT EXISTS birth_topic VARCHAR(512) NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS birth_payload TEXT NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS birth_retain BOOLEAN NOT NULL DEFAULT FALSE,
    ADD COLUMN IF NOT EXISTS birth_qos SMALLINT NOT NULL DEFAULT 0,
    ADD COLUMN IF NOT EXISTS close_topic VARCHAR(512) NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS close_payload TEXT NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS close_retain BOOLEAN NOT NULL DEFAULT FALSE,
    ADD COLUMN IF NOT EXISTS close_qos SMALLINT NOT NULL DEFAULT 0,
    ADD COLUMN IF NOT EXISTS will_topic VARCHAR(512) NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS will_payload TEXT NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS will_retain BOOLEAN NOT NULL DEFAULT FALSE,
    ADD COLUMN IF NOT EXISTS will_qos SMALLINT NOT NULL DEFAULT 0;
